<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tasty.app.module.review.repository.mapper.ReviewMapper">

  <!-- 리뷰 등록 -->
  <insert id="insertOne" useGeneratedKeys="true" keyProperty="reviewId">
    insert into review (title, content)
    values (#{title}, #{content})
  </insert>

  <!-- 리뷰 조회 -->
  <select id="selectOneById" resultType="com.tasty.app.module.review.form.ReviewForm">
    select review_id, title, content
    from review
    where review_id = #{reviewId}
  </select>
</mapper>